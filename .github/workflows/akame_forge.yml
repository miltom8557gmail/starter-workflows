name: 🔱 AKAME OMEGA FORGE
on: [push]
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # Força compatibilidade com o padrão 2026
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Código Assimilado
        uses: actions/checkout@v4

      - name: ☕ Ativando Java 21 (Estabilidade 2026)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: 🛠️ Preparando Permissões
        run: chmod +x gradlew

      - name: 🚀 Compilação Final Unificada
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: 📦 Entrega do Tesouro v52.8
        uses: actions/upload-artifact@v4
        with:
          name: Akame-Omni-v52-Final
          path: app/build/outputs/apk/debug/app-debug.apk
